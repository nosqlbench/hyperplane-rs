%% Copyright (c) Jonathan Shook
%% SPDX-License-Identifier: Apache-2.0
flowchart TB
    subgraph Authored ["1. Element Graph (authored)"]
        A["user writes in TestPlan<br/>nodes = Element structs<br/>edges = Dependency + RelationshipType<br/>axes attach here"]
    end
    subgraph Instance ["2. Element Instance Graph (compiler Stage 1)"]
        B["reducto: axis expansion +<br/>binding-state +<br/>Dedicated propagation +<br/>Lifeline collapse<br/><br/>nodes = element instances<br/>(one per unique bound param set)"]
    end
    subgraph Atomic ["3. Atomic Step Graph (compiler Stage 2, SRD-0009)"]
        C["reducto emits:<br/>Deploy, Await, SaveOutput,<br/>Teardown, Barrier<br/><br/>per-instance steps;<br/>what the executor runs"]
    end

    Authored --> Instance --> Atomic

    classDef authored fill:#e8f0ff,stroke:#3366aa
    classDef derived fill:#fff4e0,stroke:#aa7733
    class A authored
    class B,C derived
